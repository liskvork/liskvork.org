image: alpine/edge
secrets:
  - 413978ce-a9e4-4ce7-bb3b-ccff1149a7a6
environment:
  REPO: liskvork.org
  GH_USER: liskvork
tasks:
  - push-to-github: |
      cd ~/"${REPO}"
      git config --global credential.helper store
      git push --mirror "https://github.com/${GH_USER}/${REPO}"
